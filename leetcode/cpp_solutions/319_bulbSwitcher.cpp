class Solution {
public:
    int bulbSwitch(int n) {
        return sqrt(n);
    }

    // find patterns
    // if a num has even number of factors then is will be off at the end
    // otherwise, it will be on, those complete square numbers have odd number of factors
    // 如果一个数的因数个数是奇数那么就会on，不然就是off，只有完全平方的因数个数是奇数
};
